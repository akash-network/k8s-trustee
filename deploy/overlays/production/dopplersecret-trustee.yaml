apiVersion: secrets.doppler.com/v1alpha1
kind: DopplerSecret
metadata:
  name: trustee-production
  namespace: doppler-operator-system
spec:
  config: REPLACE_ME
  managedSecret:
    name: trustee-runtime
    namespace: trustee-system
    type: Opaque
  project: REPLACE_ME
  tokenSecret:
    name: trustee-production-token-secret
  verifyTLS: true
