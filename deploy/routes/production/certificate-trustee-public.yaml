apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: trustee-public
  namespace: trustee-system
spec:
  dnsNames:
    - kbs.example.invalid
  issuerRef:
    kind: ClusterIssuer
    name: REPLACE_ME
  secretName: trustee-public-tls
