apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: trustee-default-deny
  namespace: trustee-system
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: trustee
  policyTypes:
    - Ingress
    - Egress
